import { describe, expect, it } from "vitest";

// Placeholder smoke test — replaced by real suites as phases land.
describe("smoke", () => {
  it("runs the test harness", () => {
    expect(true).toBe(true);
  });
});
